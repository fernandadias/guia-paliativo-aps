# Supabase — armazenamento anonimizado dos preenchimentos (Épico F).
# Sem estas variáveis, o guia funciona normalmente e nada é enviado ao servidor.
# Crie um projeto em https://supabase.com, rode supabase/migrations/0001_fills.sql
# e copie URL + anon key do painel (Project Settings > API).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
